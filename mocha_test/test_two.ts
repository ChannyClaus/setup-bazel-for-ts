describe("Typescript usage suite", () => {
    it("should be able to execute a test two", () => {
      console.log("hie")
    });
    // it("should return expected string", () => {
    //   equal(index("incoming"), "incoming-static");
    // });
  });